## 目录
- [整理 AI 基础概念卡片](#task-ai-向任务整理-ai-基础概念卡片)
- [AI 时代的 Web3 架构能力](#task-线上活动实时参加-518ai-时代的-web3-架构能力)
- [Open Agentic Economy](#task-线上活动观看回放-523open-agentic-economy)
- [Web3基础概念](#task-web3-向任务-整理-Web3-基础概念卡片)
## Task: AI 向任务｜整理 AI 基础概念卡片

#### Agent

LLM 本身只做一件事：给定输入，输出文字。Agent 是在这之上加了一个循环——模型可以决定下一步做什么，执行，看结果，再决定下一步，直到任务完成。

关键在于"自主决策+执行"这个闭环。一个只回答问题的 ChatBot 不是 Agent；一个能自己搜索资料、写代码、跑代码、根据报错修改再跑的系统，才是。

**例子：** 让 Agent 帮你做竞品分析。它可能依次：搜索竞品官网 → 抓取内容 → 整理成表格 → 发现数据缺失 → 自己补充搜索 → 最终输出报告。全程你只给了一句指令。

**使用边界：** Agent 越自主，越难预测它会做什么。任务链越长，中间某一步出错累积的偏差越大。不适合对准确性要求极高、不可逆操作多的场景（比如直接执行交易），除非配合 human-in-the-loop。



#### Tool Use（工具调用）

模型本身没有"手"，Tool Use 给了它手。本质是：模型在生成回复时，可以输出一个结构化的"调用请求"（比如 `search("上海天气")`），外部系统执行这个调用，把结果返回给模型，模型再继续生成。

模型自己不执行工具，它只负责"决定调用什么、传什么参数"，真正跑的是外部代码。

**例子：** Claude 回答"今天上海多少度"，它不知道实时天气，但它可以调用一个 `get_weather` 工具，拿到结果再告诉你。

**误区：** Tool Use 不等于 Agent。单次调用一个工具、拿结果、结束，这是 Tool Use，不构成 Agent。Agent 是多轮、自主决策的循环，Tool Use 是 Agent 的能力组件之一。



#### AI Coding

泛指用 AI 辅助或自动化编写代码的工作方式，从 Copilot 式的行内补全，到 Cursor 式的多文件编辑，到 Claude Code 式的完整任务执行，粒度差异很大。

**更准确的分类：**
- **补全级：** 你在写，AI 帮你填（Copilot）
- **对话级：** 你描述需求，AI 生成片段，你审核粘贴
- **Agent 级：** 你给任务，AI 自主读取上下文、修改多个文件、验证结果（Claude Code、Devin）

**使用边界：** AI 对自己写的代码会"自信地犯错"——逻辑错误、幻觉 API、边界条件遗漏。粒度越大、自主性越高，需要你 review 的能力要求反而越高，不是替代你思考，是放大你的判断。



#### Guardrails（护栏）

对模型的输入或输出加约束，防止它做不该做的事，或输出不符合预期格式/内容的结果。

**护栏可以在不同层：**
- **输入层：** 过滤用户输入里的有害内容、注入攻击
- **输出层：** 检测模型输出是否合规（不含敏感词、格式正确、不乱承诺）
- **逻辑层：** 限制 Agent 能调用哪些工具、能访问哪些资源

**例子：** 金融场景里，模型不能输出"这支股票一定会涨"这类确定性建议，Guardrails 会检测并拦截或改写。

**误区：** Guardrails 不是让模型"更聪明"，是给它套边界。护栏设计不好会误杀正常输出（过严），或拦不住真正的风险（过松）。它是工程问题，不是提示词问题。



#### Tracing（追踪）

Agent 或复杂 LLM 应用跑起来之后，你看到的只是最终输出。Tracing 是记录"中间发生了什么"的机制——模型调用了哪些工具、传了什么参数、每一步耗时多少、哪一步出了错。

**类比：** 分布式系统里的链路追踪（OpenTelemetry），只不过追踪对象从微服务变成了 LLM 调用链。

**例子：** 一个 Agent 任务跑了 2 分钟，最终输出是错的。没有 Tracing，你不知道哪步出了问题；有 Tracing，你能看到"第 3 步搜索返回了空结果，第 4 步模型开始幻觉"。

**使用边界：** 本地跑个小脚本不需要 Tracing；一旦系统进入生产、任务链超过 3 步、或者要做性能优化和 debug，Tracing 就是必需品。常见工具：LangSmith、Langfuse、Arize Phoenix。


#### Human-in-the-loop（人工介入）

在 Agent 的自动化流程中，设置检查点，让人在关键决策前确认或干预，而不是全程放手让模型跑。

不是"AI 做不到所以人来做"，是主动设计的风险控制机制。

**介入粒度：**
- **审批式：** Agent 要执行高风险操作前弹出确认
- **纠偏式：** Agent 跑完一段后给你 summary，你可以说"这个方向不对，重来"
- **监控式：** 全程自动，但异常时触发人工介入

**使用边界：** 介入点设太多，自动化意义就没了；设太少，Agent 出错代价很高。设计原则是：在不可逆操作和高风险决策前设检查点，其余放行。

---

## Task: 线上活动｜实时参加 5.18｜AI 时代的 Web3 架构能力

### 1. 学到的概念、方法或案例

Web2 和 Web3 支付系统的容错逻辑是完全不同的。Web2 下出错是系统问题，数据可回滚、状态可冲正，工程师有后悔药吃；Web3 下出错直接是资产损失，交易不可逆，漏洞即归零。这个对比让我重新理解了"严谨性"的含义——不是流程上的谨慎，而是部署前就必须正确，因为没有补救机会。

### 2. 一个你准备采取的行动

深入了解 Web3 架构中安全设计的具体机制，包括多签、时间锁、可升级合约等模式，建立对链上系统工程严谨性的具体认知。

### 3. 还想继续追问的问题

**当智能合约的执行是自动化的、不可逆的，human-in-the-loop 的介入点应该设计在哪里？**

Web3 里 human-in-the-loop 的核心设计哲学是：把所有需要人判断的事情，挪到链上执行之前完成。主要介入点包括：

- **合约部署前**：代码审计（人工 + 自动化工具）、形式化验证、测试网验证。最高价值的介入点，合约部署后逻辑固化，无法修改。
- **多签机制（Multisig）**：高风险操作需要 M-of-N 个地址共同签名才能执行，把人的确认编进合约逻辑本身。
- **时间锁（Timelock）**：重要变更提交后等待窗口期（如 48 小时）才生效，给社区和安全研究员留出人工介入空间。
- **可升级合约 + 治理**：通过代理模式保留逻辑替换能力，但替换需经过 DAO 投票或多签批准。
- **熔断机制（Circuit Breaker）**：异常交易触发自动暂停，等待人工处理——唯一一个"执行中"的介入点，但本质仍是提前写好的规则。

AI 的角色：部署前辅助审计代码、运行时监控链上异常并预警、治理阶段帮助分析提案风险，最终决策仍由人完成。

容错逻辑与传统金融完全相反——后者大量依赖事后冲正和人工复核，Web3 的容错靠的是出错前拦截。

---

## Task: 线上活动｜观看回放 5.23｜Open Agentic Economy

### x402,ERC-8004,ERC-8183

x402        → 支付层（Agent 怎么付钱）

ERC-8004    → 身份层（Agent 是谁、可不可信）

ERC-8183    → 商务层（Agent 怎么接活、交付、结算）

- x402（支付层）
复活 HTTP 402 状态码，让 AI Agent 在访问付费 API 时直接用稳定币完成即时支付，无需人工介入。解决的是"Agent 怎么花钱"。

- ERC-8004（身份+信誉层）
为 AI Agent 建立身份、信誉、验证三个链上注册表。每次 Agent 通过 ERC-8183 完成任务，记录会写入其 ERC-8004 身份，随时间累积出可携带的链上信誉评分——类似 AI 工作者的信用分，任何人可验证，且没有单一平台能撤销。

- ERC-8183（商务/托管层）
由 Virtuals Protocol 和以太坊基金会 dAI 团队于 2026 年 2 月提出，定义了 Agent 之间雇佣、交付、结算争议的完整流程，无需人类中间人。三个角色：Client（发包方）、Provider（接包方）、Evaluator（独立评估方）。资金锁入智能合约托管，只有中立评估者确认交付完成后才释放。

### ZKML（Zero-Knowledge Machine Learning） 
用 ZK 证明来验证"某个 AI 模型确实跑了某个推理，且结果是这个"，而不暴露模型权重或输入数据。核心价值是让 AI 计算可验证。
### ZK LM Credits
基于 ZKML 的衍生概念，把 AI 推理算力/使用权益代币化，用 ZK 证明背书每一次推理确实发生了。

---

## Task: Web3 向任务｜整理 Web3 基础概念卡片

### 1. Signature（签名）

**一句话**：用私钥证明"这个操作是我本人授权的"，不需要密码，不需要第三方。

**误区**：签名不等于"只是确认一下"——有些签名会直接授权合约转走你所有代币，看不懂内容就签是最危险的操作之一。


### 2. Transaction（交易）

**一句话**：你向链上提交的一个状态变更请求，签名后广播，矿工/验证者打包进区块后永久生效。

**误区**：交易一旦上链无法撤销。发错地址、金额填错，没有任何"客服"可以帮你追回。


### 3. Gas

**一句话**：在以太坊上执行任何操作需要支付的计算费用，以 Gwei 计价。

**误区**：Gas 费用和交易金额无关——转 0.001 ETH 和转 100 ETH 的 Gas 费一样，Gas 衡量的是计算复杂度，不是金额大小。


### 4. Seed Phrase（助记词）

**一句话**：12 或 24 个英文单词，是你整个钱包的终极备份，能推导出所有私钥和账户。

**例子**：MetaMask 创建钱包时让你抄下来的那组单词，丢了就永久失去钱包控制权。

**安全提醒**：任何网站、任何"客服"、任何人要求你输入助记词，100% 是诈骗。助记词只在本地离线手抄，永远不截图、不存云盘、不发给任何人。

### 5. Smart Contract（智能合约）

**一句话**：部署在链上的自动执行程序，条件满足就运行，没有人能中途干预。

**例子**：Aave 的借贷合约：你存入 ETH，合约自动计算利息、开放借款额度；健康系数低于 1 自动触发清算，不需要任何人工操作。

**误区**：合约代码公开不等于安全——公开是可审计，但有漏洞的合约也是公开的，被攻击前任何人都能看到代码。

### 6. Testnet（测试网）

**一句话**：和主网结构完全一样的沙盒环境，使用没有真实价值的测试币，专门用于开发和学习。

**例子**：你在 Base Sepolia 上做的 Aave 存款、Revoke.cash 撤销授权，全部用的是测试 ETH，不涉及真实资产。

**误区**：测试网的代币没有价值，但操作习惯和安全意识要和主网一致——测试网是练手，不是"随便搞"。

### 7. Block Explorer（区块浏览器）

**一句话**：链上数据的搜索引擎，可以查询任意地址、交易、合约的完整历史记录。

**例子**：在 sepolia.basescan.org 搜索你的钱包地址，可以看到所有交易记录、token 持仓、合约交互历史。

**误区**：区块浏览器显示"成功"只代表源链交易成功，跨链场景必须去目标链的浏览器单独验证到账。


### 8. EOA / Smart Account / Multisig

**EOA（Externally Owned Account）**
普通用户钱包，由私钥控制，MetaMask 默认创建的就是 EOA。简单直接，但私钥丢失即失去一切。

**Smart Account（智能账户）**
合约钱包，账户本身是一个智能合约，可以设置社交恢复、批量交易、无 Gas 签名等功能。ERC-4337 是目前主流标准。

**Multisig（多签钱包）**
需要多个私钥共同签名才能执行操作，例如 2-of-3 多签需要 3 个密钥中的任意 2 个同意。常用于团队金库、DAO 资金管理。

**误区**：多签更安全，但不是零风险——如果多个签名方同时被社会工程攻击，多签也可能被绕过。

### 特别说明：私钥、助记词、签名、授权为什么必须谨慎

任何一层被攻破，后面全部失效。

| 层级 | 泄露后果 | 常见攻击手段 |
|---|---|---|
| 助记词 | 钱包全部资产永久丢失 | 钓鱼网站、假客服索取 |
| 私钥 | 同上 | 恶意软件、截图泄露 |
| 签名 | 单次授权被滥用 | 仿冒网站诱导签名 |
| Approve 授权 | 合约可随时转走代币 | 貌似正常的 DApp 交互 |
